{
  "schemaVersion": "1.0.0",
  "projectStateVersion": 3,
  "summary": "BLE dashboard app that connects to an ESP32 device via Bluetooth and displays sensor data. Parses pipe-delimited string format (BAT:85|LAT:2050|VAL:450) to show battery level, actuation latency, and real-time light level chart.",
  "architectural_notes": [
    "ESP32 sends BLE data as pipe-delimited string: BAT:{battery%}|LAT:{latency_ms}|VAL:{light_level}",
    "BLE parsing: split on '|', extract numeric values after BAT:, LAT:, VAL: keys"
  ],
  "known_issues": [
    "Dashboard cards are empty due to unimplemented ESP32 string format parsing"
  ],
  "isFixRequest": true,
  "existing_features": [
    {
      "id": "feature-in-frontend-src-utils-blenus-ts-ensure-the-esp32-pipe-delimited-ble-string-parser-splits-the-incoming-string-by-then-extracts-the-numeric-value-after-bat-lat-and-val-prefixes-respectively-returning-them-as-structured-fields-e-g-battery-number-latency-number-val-number-the-parser-must-handle-the-exact-format-bat-85-lat-2050-val-450",
      "title": "In `frontend/src/utils/bleNus.ts`, ensure the ESP32 pipe-delimited BLE string parser splits the incoming string by `|`, then extracts the numeric value after `BAT:`, `LAT:`, and `VAL:` prefixes respectively, returning them as structured fields (e.g., `{ battery: number, latency: number, val: number }`). The parser must handle the exact format `BAT:85|LAT:2050|VAL:450`.",
      "reqIds": [
        "REQ-1"
      ],
      "version": 1
    },
    {
      "id": "feature-in-frontend-src-contexts-bluetoothcontext-tsx-update-the-ble-notification-handler-so-that-when-a-notification-arrives-it-invokes-the-pipe-delimited-parser-from-blenus-ts-and-stores-the-resulting-battery-latency-and-val-fields-in-the-context-state-replacing-any-prior-ad-hoc-parsing-of-these-values",
      "title": "In `frontend/src/contexts/BluetoothContext.tsx`, update the BLE notification handler so that when a notification arrives it invokes the pipe-delimited parser from `bleNus.ts` and stores the resulting `battery`, `latency`, and `val` fields in the context state, replacing any prior ad-hoc parsing of these values.",
      "reqIds": [
        "REQ-2"
      ],
      "version": 1
    },
    {
      "id": "feature-update-frontend-src-components-device-batteryindicator-tsx-to-consume-the-battery-value-0-100-from-bluetoothcontext-and-display-it-in-the-battery-card-the-card-must-show-the-numeric-percentage-and-update-live-as-new-ble-notifications-arrive",
      "title": "Update `frontend/src/components/device/BatteryIndicator.tsx` to consume the `battery` value (0–100) from `BluetoothContext` and display it in the Battery card. The card must show the numeric percentage and update live as new BLE notifications arrive.",
      "reqIds": [
        "REQ-3"
      ],
      "version": 1
    }
  ],
  "new_feature_requests": [
    {
      "id": "AR-1",
      "summary": "Parse ESP32 BLE string format BAT:|LAT:|VAL: and display values in dashboard cards and chart",
      "reqIds": [],
      "status": "pending"
    },
    {
      "id": "AR-5",
      "summary": "Update `frontend/src/components/device/ActuationLatencyCard.tsx` to consume the `latency` value (milliseconds integer) from `BluetoothContext` and display it in the Actuation Latency card. The card must show the live value as new BLE notifications arrive.",
      "reqIds": [
        "REQ-4"
      ],
      "status": "pending"
    },
    {
      "id": "AR-6",
      "summary": "Update `frontend/src/components/charts/BlinkRateChart.tsx` to feed the `val` field (light level) from `BluetoothContext` into the real-time light level chart. Each incoming BLE notification should append a new data point (timestamp + `val` value) to the chart's rolling data series.",
      "reqIds": [
        "REQ-5"
      ],
      "status": "pending"
    }
  ],
  "gameProjectType": "none",
  "projectName": "EyeR Sleep Monitor",
  "clarification_mode": "instant",
  "clarification_rounds": 0
}